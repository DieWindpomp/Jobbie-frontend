<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Jobcard.Views.Details.JobDetail">
    <StackLayout x:Name="MainLayout">
        <Label x:Name="lbl_NoInternet" HorizontalTextAlignment="Center" TextColor="White" BackgroundColor="Red"/>
        <ActivityIndicator IsRunning="False" Color="LightBlue" x:Name="ActivitySpinner"
                                   VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"
                                   RelativeLayout.XConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Factor=0.45}"
                                   RelativeLayout.YConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height, Factor=0.33}" />
        
        <Label x:Name="lblJobHeader" HorizontalTextAlignment="Start" FontSize="Large"/>
        <Label x:Name="lblJobLocation" HorizontalTextAlignment="Start" FontSize="Large"/>
        <Label x:Name="lblJobDescription" HorizontalTextAlignment="Start" FontSize="Large"/>
        <Label x:Name="lblViewOnMaps" Text="" HorizontalTextAlignment="Start" FontSize="Large"/>
        <Label x:Name="lblClientName" HorizontalTextAlignment="Start" FontSize="Medium"/>
        <Label x:Name="lblComment" HorizontalTextAlignment="Start" FontSize="Medium"/>
        <Grid BackgroundColor="White" VerticalOptions="EndAndExpand">
            <ImageButton Grid.Column="0" x:Name="btnBack"/>
            <Picker Grid.Column="1" x:Name ="pickAction" Title="Choose Action">
                <Picker.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>Complete</x:String>
                        <x:String>Comment</x:String>
                        <x:String>Cancel</x:String>
                        <x:String>Delete</x:String>
                        <x:String>View On Map</x:String>
                    </x:Array>
                </Picker.ItemsSource>
            </Picker>
            <ImageButton Grid.Column="2 " x:Name="btnConfirm" Clicked="btnConfirm_clicked"/>
        </Grid>



    </StackLayout>
</ContentPage>