<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Jobcard.Views.Details.CompletedJobs">
    <StackLayout x:Name="MainLayout">
        <Label x:Name="lbl_NoInternet" HorizontalTextAlignment="Center" TextColor="White" BackgroundColor="Red"/>

        <ActivityIndicator IsRunning="True" Color="LightBlue" x:Name="ActivitySpinner"
                                   VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"
                                   RelativeLayout.XConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Factor=0.45}"
                                   RelativeLayout.YConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height, Factor=0.33}" />
        
        <Label   x:Name="lblEmployee" Text="Select Employee" FontSize="Medium" FontAttributes="Bold" IsVisible="True" HorizontalOptions="FillAndExpand"/>
            <Picker  x:Name="pickEmployee" Title="Select Employee" IsVisible="True" SelectedIndexChanged ="PickEmployee_SelectedIndexChanged" HorizontalOptions="FillAndExpand"/>
            
            <Label x:Name="lblNoItems" Text="No Jobs in List" HorizontalTextAlignment="Center" IsVisible="False"/>
            
            <ListView x:Name="listview" SeparatorVisibility="Default">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid BackgroundColor="White">
                                <Label Grid.Column="0" HorizontalOptions="StartAndExpand" VerticalOptions="StartAndExpand" Text="{Binding Description}" TextColor="Black" FontSize="Small"/>
                            <Label Grid.Column="1" HorizontalOptions="StartAndExpand" VerticalOptions="StartAndExpand" Text="{Binding CompanyName}" TextColor="Black" FontSize="Small"/>
                        </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
            

    </StackLayout>
</ContentPage>